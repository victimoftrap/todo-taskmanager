server:
  port: 8080
spring:
  flyway:
    baseline-on-migrate: true
  datasource:
    tasks:
      jdbc-url: jdbc:postgresql://localhost:5432/eisetasks
      username: someuser
      password: somepass
      driver-class-name: org.postgresql.Driver

jwt:
  issuer: 'some.admin@mail.net'
  signingKey: 'Zcps300xgpKs8rgc6lrb'
  aTokenDuration: 30